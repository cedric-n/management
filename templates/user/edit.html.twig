{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('home') }}
    {{ encore_entry_link_tags('form') }}
{% endblock %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <main class="container d-flex justify-content-center align-items-center">
        <div class="row w-100 h-100 d-flex justify-content-center align-items-center">

            <div class="content-form w-100 d-flex flex-column justify-content-center align-items-center">
                <div class="head-form border-head d-flex justify-content-center align-items-center text-center border  py-3">
                    <h1 class="text-white">Modification du compte utilisateur</h1>
                </div>
                <div class="body-form form-border d-flex flex-column justify-content-center align-items-center border  bg-white w-1OO py-3">

                    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
                <a href="{{ path('userinterface_index') }}">Retour à l'accueil utlisateur</a>
            </div>
        </div>
    </main>
{% endblock %}
