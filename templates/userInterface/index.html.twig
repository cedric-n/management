{% extends 'base.html.twig' %}

{% block stylesheets %}
        {{ parent() }}
        {{ encore_entry_link_tags('home') }}
        {{ encore_entry_link_tags('userInterface') }}
{% endblock %}


{% block body %}
        <main>
                {{ include('userInterface/_sidebars.html.twig') }}
                <div class="info-content w-100 ">
                        <div class="d-flex justify-content-between text-center bg-light w-100">
                                <div class="case-user w-25 border-right border-3 bg">
                                        <h3>{{ app.user.firstname }}</h3>
                                        <svg aria-hidden="true" focusable="false" data-prefix="fas" data-icon="user-circle" class="svg-inline--fa fa-user-circle fa-w-16" role="img" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 496 512"><path fill="currentColor" d="M248 8C111 8 0 119 0 256s111 248 248 248 248-111 248-248S385 8 248 8zm0 96c48.6 0 88 39.4 88 88s-39.4 88-88 88-88-39.4-88-88 39.4-88 88-88zm0 344c-58.7 0-111.3-26.6-146.5-68.2 18.8-35.4 55.6-59.8 98.5-59.8 2.4 0 4.8.4 7.1 1.1 13 4.2 26.6 6.9 40.9 6.9 14.3 0 28-2.7 40.9-6.9 2.3-.7 4.7-1.1 7.1-1.1 42.9 0 79.7 24.4 98.5 59.8C359.3 421.4 306.7 448 248 448z">
                                                </path>
                                        </svg>
                                </div>
                                <div class="case-user w-25 border-right border-3">
                                        <h3>Somme total des retrait :</h3>
                                        {% for info in infoType0 %}
                                                <p>{{ info.total }}</p>
                                        {% endfor %}
                                </div>
                                <div class="case-user w-25 border-right border-3">
                                        <h3>Somme total des entrées:</h3>
                                        {% for info in infoType1 %}
                                                <p>{{ info.total }}</p>
                                        {% endfor %}
                                </div>
                                <div class="case-user w-25">
                                        <h3>Portefeuille:</h3>
                                        {% if wallet >= 0 %}
                                        <p class="text-info">{{ wallet }}</p>
                                        {% else %}
                                                <p class="text-danger">{{ wallet }}</p>
                                        {% endif %}
                                </div>

                        </div>

                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        {{ form_end(form) }}
                        {{ render_chart(chart, {'class': 'my-chart  h-50 w-100'}) }}
                </div>
        </main>
{% endblock %}
